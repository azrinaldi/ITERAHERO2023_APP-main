import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop: 60,
        alignItems: 'center',
        height: '85%',
        width: '100%',
        justifyContent: 'space-between',
        flexDirection: 'column'
    },
    content: {
        width: '80%',
        // height: '100%',

    },
});

export default styles