import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    card: {
        width: '100%',
        backgroundColor: '#ffff',
        borderRadius: 10,
        borderColor: '#171717',
        elevation: 1,
        shadowOpacity: 1,
        shadowRadius: 1,
        marginBottom: 10,
        padding: 10,
        justifyContent: 'space-between',
    },
    switch: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center'
    },
    waktu: {
        color: ''
    }
});

export default styles